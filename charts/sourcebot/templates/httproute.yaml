{{- if $.Values.sourcebot.httpRoute.enabled -}}
{{- if not $.Values.sourcebot.httpRoute.parentRefs -}}
{{- fail "sourcebot.httpRoute.parentRefs is required when sourcebot.httpRoute.enabled is true. An HTTPRoute without parentRefs is not attached to any Gateway and silently receives no traffic." -}}
{{- end -}}
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "sourcebot.fullname" $ }}
  namespace: {{ $.Release.Namespace }}
  labels:
    {{- include "sourcebot.labels" $ | nindent 4 }}
  {{- with $.Values.sourcebot.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with $.Values.sourcebot.httpRoute.parentRefs }}
  parentRefs:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with $.Values.sourcebot.httpRoute.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- with $.Values.sourcebot.httpRoute.rules }}
    {{- toYaml . | nindent 4 }}
    {{- else }}
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "sourcebot.fullname" $ }}
          port: {{ $.Values.sourcebot.service.port }}
    {{- end }}
{{- end }}
